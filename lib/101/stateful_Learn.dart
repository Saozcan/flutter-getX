import 'package:flutter/material.dart';

class StatefullLearn extends StatelessWidget {
  const StatefullLearn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("Ahmet"),
    );
  }
}
